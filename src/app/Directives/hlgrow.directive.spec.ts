import { HlgrowDirective } from './hlgrow.directive';

describe('HlgrowDirective', () => {
  it('should create an instance', () => {
    const directive = new HlgrowDirective();
    expect(directive).toBeTruthy();
  });
});
